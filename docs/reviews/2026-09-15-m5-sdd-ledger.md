# SDD ledger — plan: 

Spec: tt-vga-capture/docs/superpowers/specs/2026-09-15-tt-vga-capture-design.md (read). Inputs: M3 final review recommendations (docs/reviews/2026-09-15-m3-final-review.md in the tracking repo).

## Pre-flight scan (2026-09-15)

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 (C frame lib) vs T2 (Python) | disjoint files | can run in parallel |
| T2 -> T4 | iter_capture / CaptureSession / `ttcap capture --out -` | T4 spawns the CLI; contract = header first then chunks, stderr only for messages |
| T3 -> T4/T5 | vgadecode caps and properties | fixed in the plan |
| T3 vs T1 | vgadecode wraps libvgaframe; T1 changes learner internals only | API unchanged (new `glitches` field) |

Rulings: none yet.

## Progress

Task 1: implementer DONE (e99101f..83fe030; ctest 8/8, pytest 261). tt08 Tiny Logo capture now yields a complete frame (glitches=19). Deviations to judge: trailing-edge line start with report_x, give-up valve after 16 rejections. Review dispatched (opus).
Task 1: review — spec PASS, quality PASS, no Critical/Important; x-origin verified unshifted (head frames byte-identical to base on cap_fpga1_min3). 7 minors. Ruling: fix the two that weaken the new regression test (fixture pytest inspects no pixel; per-frame glitches line is a lifetime total); defer the other five. Fix round 1/5 dispatched.
Task 1: minor (deferred): valve refinements (2), lost-line counting (round(distance/cpl)), phase-locked distance test, 25% window accepting an early edge (documented as specified behaviour).
Task 2: dispatched (opus) on a worktree from main: stream_header_bytes, iter_capture, CaptureSession, RawRepl write lock, stop predicate, FRAM accounting, ttcap capture --out -.
Task 1: fix round 1/5 re-review clean (values independently recomputed). Task 1: complete (e99101f..7f797c5, merged to main --no-ff at 20421f9). Controller fixed the stale README glitch paragraph noted out-of-scope.
Task 3: dispatched (opus) on a worktree from ea4b98a: gst/ plugin with vgadecode, tests via gst-launch compared pixel-exactly against vgacap-frames. GStreamer 1.28.2 with headers is present on the workstation.
Task 2: implementer COMPLETE (c803af1, 31c3d8b, 2cad105; 295 tests). Hardware on fpga-1: `--out -` produced a clean 3,886,029-byte stream on stdout with all messages on stderr, 8 frames reconstructed; an early-closing consumer (head -c 500000) got its bytes, the capture ended with a clean BrokenPipeError and the board was immediately usable (probe OK). Review dispatched.
Observation for Task 4: a broken pipe is reported as "capture failed"; for a GStreamer source that stops consuming it is the normal end, so the exit path should distinguish them.
Task 3: implementer COMPLETE (0bd3e51..2aef36c on m5-task3-gstvgadecode; 14 gst tests, 275 pytest, ctest 9/9). Controller verified on real data: plugin builds, gst-inspect lists vgadecode, and `filesrc ! vgadecode ! videoconvert ! pngenc ! multifilesink` over the fpga-1 tt_um_vgacal_bars capture produced 11 PNGs with frame 5 pixel-identical to the calibration reference. Review dispatched (opus).
Task 2: review — spec PASS, quality PASS with 2 Important: (1) BrokenPipeError exits 1 as a failure and can force exit 120 on shutdown flush; (2) _send_stop releases the lock before writing the stop byte, so a cross-thread stop can land on the next command (commit 2cad105 has zero coverage, proven by neutering it). 10 minors. Fix round 1/5 dispatched with those two plus three minors (stop-latency docs on request_stop and --help, SIGINT fallback predicate, SIGINT promptness).
Task 3: review — spec PASS, quality PASS WITH CHANGES. Important: (1) PTS rescaled by the current clock_hz over a since-byte-0 accumulator, so a mid-stream rate change makes PTS run backwards (reproduced 84ms -> 16.8ms), breaking matroskamux and Task 5's MKV leg; (2) -Werror/-Wshadow dropped without cause (measured: only -Wconversion fires, twice, inside GStreamer headers). OFFSET-for-frame-counter upheld as correct (GStreamer documents it as the frame number). 12 minors. Fix round 1/5 dispatched with both Important plus the mute-after-flush bug (vgaframe_reset), the OFFSET reset documentation, the unrecognised licence string and the renegotiation test.
Task 2: fix round 1 DONE (6e3adb4, 197879d; 303 tests). Controller ran the two hardware checks the implementer could not: early-closing consumer now exits 0, prints the stats to stderr and "capture ended: the consumer closed the stream", with the board stopping cooperatively after 1.3 s instead of the full 30; and `--out -` at 750 kHz piped through gzip sustained 730,714 samples/s with zero overruns, 7.34 MB streamed, 17 frames reconstructed. Scoped re-review dispatched.
Task 3: fix round 1 DONE (b68a73f..b3db96a; ctest 9/9, 278 pytest, 17 gst tests, build clean with -Werror -Wshadow). Implementer found Minor 3's real cause a level below vgaframe: the reader rejects chunks with "chunk before header" after a flush, so it re-encodes the cached header on FLUSH_STOP. Controller verified on hardware data: `filesrc ! vgadecode ! videoconvert ! x264enc ! matroskamux ! filesink` produced a valid MKV, 640x480, duration 9.240 s and frame rate 25/21, which is exactly 11 frames of 420,000 clocks at 500 kHz, i.e. the video carries the project's own time base; it demuxes back to 11 frames. Scoped re-review dispatched.
Task 3: fix round 1 re-review PASS (verified independently across 4 rate changes and a second header; fresh configure clean with -Werror -Wshadow). Task 3: complete (0bd3e51..b3db96a, merged to main --no-ff at e80fe84).
Task 3: minor (deferred): 12 listed in task-3-report.md's ledger, incl. RESYNC only logged to GST_DEBUG, and a flush re-announcing the same timing message.
Task 2: fix round 1 re-review Approve (both mutations re-run independently). Task 2: complete (c803af1..197879d, merged to main --no-ff at c25be83; merged cleanly with Task 3). Full tree: ctest 9/9, 321 pytest, 17 gst tests.
Task 2: minor (deferred): a consumer disconnecting at the same moment the board errors still exits 0 (the stats line carries the error to stderr); plus the 6 the implementer deferred.
Task 4: dispatched (opus) from c25be83: vgacapttsrc (spawns ttcap capture --out -, cooperative SIGINT stop, stderr surfaced as errors) and vgacapbin (URI parsing), with a fake ttcap for tests.
Task 4: implementer DONE (41c824e..63a3e0d; ctest 12/12, 345 pytest, 39 gst tests). Controller ran it live against fpga-1 through the bridge:
 - `vgacapttsrc ! vgadecode ! videoconvert ! pngenc ! multifilesink` produced 11 frames and frame 5 is pixel-identical to the calibration reference, so the LIVE path is correct end to end.
 - `vgacapbin uri=tt-ws://...` to `x264enc ! matroskamux` produced a 258 KB MKV, 640x480, duration 9.240 s, 25/21 fps (the project's own time base).
 - Stopping a `seconds=0` capture by pipeline teardown took ~4 s total and left no orphaned child.
 - Usability finding: gst-launch needs the URI double-quoted (`uri="\"$URI\""`) because of the `?` and `&`; document it.
Review dispatched (opus).
Task 4: review — spec PASS, quality PASS with required fixes. Important 1: stop() can hang for ever (signal_child bails once the leader is reaped, so a descendant holding the pipes is never killed, and g_thread_join then waits on an EOF that never comes; reproduced, >90 s). Important 2: SECURITY — a vgacapbin URI query can set ttcap-command, i.e. arbitrary command execution, which becomes RCE once Task 5's browser view accepts URIs (reproduced). Important 3: a PAUSED pipeline gives the board only one pipe buffer (65,548 bytes, ~90 ms at 750 kHz) before overrunning, and the overrun count is lost with the discarded trailer; documentation fix. The reviewer also verified the uv-run SIGINT hop works. 12 minors. Fix round 1/5 dispatched.
Ruling: Task 5 does not start until Important 2 is fixed and re-reviewed; a browser-facing feature must not be built on a URI parser that can set an executable path.
Task 4: fix round 1 DONE (bd75388, 36eb447, 6bd67e2; ctest 12/12, 350 pytest, 46 gst tests; each fix pinned by a revert-test). Controller verified on hardware: the live board pipeline still yields 11 frames in 11 s; the injection URI is now refused with a clear error naming the disallowed key and the sentinel file was never created; allowed query keys still drive a real capture. Scoped re-review dispatched.
Task 4: fix round 1 re-review PASS with one new required fix: the pgid guard was over-applied, so on the ordinary EOS path step 1 signals a possibly-recycled process group (strace: reap, then kill(-pid) 8 s later; dwell is unbounded). Regression of the round, two-line fix. The reviewer independently reproduced the pre-fix hang, refused six smuggling URIs, and ran 25 start/stop cycles with flat fds and RSS. Fix round 2/5 dispatched.
Task 4: fix round 2 DONE (9b71a40): waitid(WNOWAIT) keeps the leader unreaped so the pgid is pinned while the element may still signal, reaping moved to a step 6 after the last signal, plus a group_is_live() guard on step 1; new strace-based test fails on any kill after EOS. Controller re-ran the live pipeline: 11 frames in 12 s, zero zombies two seconds later. Scoped re-review dispatched, asked to check for a per-element zombie leak on paths that never call stop().
Task 4: fix round 2 re-review PASS (mutant build reproduced the regression, guard proven load-bearing, no zombie leak). Task 4: complete (41c824e..9b71a40, merged to main --no-ff at 6cb248a). Full tree: ctest 12/12, 351 pytest, 47 gst tests.
Task 4: minor (deferred): 12 in the report ledger (6 and 9 closed incidentally).
Task 5: dispatched (opus) from 6cb248a: ttcap demo with PNG, video, optional window and an MJPEG browser view; source built programmatically so ttcap-command never comes from a URI.
Task 5: implementer COMPLETE (fb9b363..75ce8cf; 380 pytest, 47 gst tests) but `ttcap demo --help` crashes with TypeError: argparse interpolates help strings and `frame-%04d.png` is an unescaped placeholder; no test covered --help. Fix round 1/5 dispatched (escape, plus a test that exercises --help for every subcommand from the parser's own list, plus --fps for repeat-last-frame/output-fps). Two ALLOW_MEGACOMMIT=1 overrides flagged honestly by the implementer; accepted, prefer file splits next time.
Task 5: fix round 1 (help crash, --fps) DONE (11361e0, 1326ade). Controller ran the demo on fpga-1: 51 PNGs, capture.mkv 640x480 10.2 s at 5/1 matching --fps, frame 25 pixel-identical to the reference, and --serve delivered 32 JPEG parts to curl; results committed to tt-vga-capture/docs/results/2026-09-15-demo-fpga1/.
Task 5: review — spec PASS, quality CHANGES REQUESTED, 6 Important: F1 orphaned gst-launch/ttcap holding a shared board on SIGTERM/SIGHUP; F2 zero-file run reported "6 png(s)" and exit 0 by globbing an outdir with old frames; F3 the likeliest setup mistake (plugin not on GST_PLUGIN_PATH) gets the worst message though the right wording exists two lines away; F4 --serve bind errors name neither flag nor port; F5 contradictory --project/--design combinations only fail at the board; F6 tunnel hint leads with a privileged port. 11 minors. Reviewer verified by experiment: --dry-run inert, MJPEG localhost-only with no thread/fd growth over ten viewer churns, one and two Ctrl-Cs both leave a playable mkv. Fix round 2/5 dispatched.
Task 5: fix round 2 DONE (cd81393, b115a99, f7bfb36; 447 pytest, 47 gst). Controller verified on hardware:
 - the demo still runs clean (43 frames, capture.mkv 585 KiB, mode reported, glitches 0);
 - SIGTERM to the demo leaves ZERO leftover capture or gst-launch processes (F1 fixed, the shared-board risk closed);
 - a non-empty outdir is refused naming the file count, the first names, the consequence and --force (F2 fixed);
 - the plugin-not-found path now prints how to build it, how to point GST_PLUGIN_PATH at it, and that it is currently unset (F3 fixed, and it was the worst message before).
Round 3 (numeric argument validation, F7: --seconds -5 previously an unbounded capture) dispatched; its two departures (fpga- prefix for board-kind checks, warn-not-refuse on --dry-run without the plugin) accepted.
Task 5: fix round 3 DONE (1ecb6f7; 487 pytest, 47 gst; 17 tests fail with the range check disabled). Controller verified on hardware: `--seconds -5` refused before anything runs and the outdir is not created; `--clock-hz 1000000` on fpga-1 warns it is above the measured 750 kHz ceiling, says overruns are expected and reported in the closing TIME chunk, and proceeds. Answer to the implementer's question: 60 kHz and 750 kHz are today's measured figures (docs/research/2026-09-15-micropython-capture-rate.md) and stand for this firmware; keying the ceiling on the board profile is right because link throughput really is a demo-board property. Scoped re-review of rounds 2-3 dispatched.
Task 5: rounds 2-3 re-review — F1-F8 all ADDRESSED; attacks held (teardown cannot hang/double-kill/fire after fork, --force cannot escape its pattern even via symlinks, 20 malformed numeric forms all refused, precedence unchanged except the intended numbers-before-plugin). Thresholds confirmed correct and profile-keyed correctly. NEW Important N1: _summarise has no evidence for the --window branch, so --window-only and short --serve-only runs report "nothing was captured" and exit 3 despite working — F2's fix overshooting into the opposite lie. Residual: SIGQUIT still orphans (one word). Plus 4 minors. Fix round 4/5 dispatched (N1, SIGQUIT, two wording corrections on the ceiling warning).
Task 5: fix round 4 DONE (8cdd702; 511 pytest, 47 gst; 4 tests fail with the verdict inverted). N1 fixed structurally: one boolean became an Outcome of three questions (made / conclusive / saw_frames). SIGQUIT added with a test pinning the set. Both ceiling wordings corrected; 3 of 4 minors fixed, the exit-code one documented with sound reasoning (a graceful stop really succeeded, so 143 would report failure for a capture that worked) — controller accepts. Hardware: a serve-only run publishing 5 frames now exits 0 and says so. Final scoped re-review dispatched (eight-combination check on the new Outcome).
